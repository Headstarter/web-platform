name: Build under Windows

on: [push]

jobs:
  build-on-windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v1
      - name: setup
        run: echo "TODO"
        shell: bash